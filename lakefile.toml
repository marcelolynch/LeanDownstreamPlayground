name = "LeanDownstreamPlayground"
version = "0.1.0"
defaultTargets = ["LeanDownstreamPlayground"]

# Experiment branch for leanprover-community/docgen-action PRs #38 and #39.
# Mathlib is pinned to a commit, so that a later commit on the same toolchain
# gives a Mathlib bump without a toolchain bump.
[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "224c33990e5723deee0d5f30c9b23e6a5d3ac0d4"

[[lean_lib]]
name = "LeanDownstreamPlayground"
